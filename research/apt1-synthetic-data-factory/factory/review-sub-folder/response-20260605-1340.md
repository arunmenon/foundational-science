# Response to review-20260605-134036-IST

All findings actioned. Verified: `python3 -m pytest -q` → **13 passed**; `python3 run.py` → golden **4/4**.

| # | Finding | Resolution | Status |
|---|---------|-----------|--------|
| 1 (HIGH) | `fully_evaluated` proved only loaded-subset coverage, not the canonical pack | Added a **certification manifest** `ATO_REQUIRED_POLICY_IDS = ATO-P01..P13` (pack_ato). `grade(..., required_ids=)` computes `missing_policy_ids = required − evaluated`; `fully_evaluated`/`certified_gold` require the **full manifest** evaluated + no errored policy. Compliant PASS now reports `missing=[P05,P06,P07,P08,P10,P11,P12,P13]`, `certified_gold=False`. Test: `test_compliant_passes_but_not_gold_until_full_manifest`. | ✅ fixed |
| 2 (HIGH) | Required-arg check didn't enforce types/enums → lowercase `sms` bypass | `_validate_args` now enforces the JSON-schema subset: types, **enums (case-sensitive)**, array item enums, and `additionalProperties=false`. `'sms'` → `protocol_error` (rejected, not normalized). Defense-in-depth: ATO-P03 also compares **case-insensitively**. Tests: `test_lowercase_channel_is_rejected_not_normalized`, `test_p03_is_case_insensitive_in_grading`. | ✅ fixed |
| 3 (HIGH) | Persisted "model-visible" log embedded hidden answer-key state | Split views: `generate_trajectory` returns `transcript` (model-visible: system/user/assistant/tool) **and** `log` (internal audit trace w/ state snapshots). `generate.py` stores `model_transcript` + `audit_trace` with correct labels. Test asserts `changed_at_hours` is in the audit trace but **not** the transcript. Test: `test_model_transcript_excludes_hidden_ground_truth`. | ✅ fixed |
| 4 (MED) | pass^k excluded errored attempts | Errors now append `False` to `pass_flags`; `pass@1` and `pass^k` computed over **all K attempts**; error count reported separately. | ✅ fixed |
| 5 (MED) | `ATO-LIFE-01` violated the policy-ID schema | Lifecycle moved OUT of `ATO_POLICIES` into a **CORE checker** in grader (`_lifecycle_violation`, id `CORE-LIFECYCLE`), so the ATO `-P[0-9]{2}` namespace is untouched. Test: `test_lifecycle_is_core_not_a_domain_policy`. | ✅ fixed |
| 6 (LOW) | Error provenance incomplete; colliding run IDs | All runs (incl. exceptions) route through a shared provenance constructor with a **batch UUID** + timestamp + model/temps/versions + prompt/tool-schema versions + terminal_reason; output files are namespaced by batch. | ✅ fixed |
| obs | `adherence_score=1.0` while P13 skipped could mislead | Added `adherence_complete` (False when any soft policy skipped/errored) so 1.0 is not mistaken for complete soft adherence. | ✅ added |

Net: certification is now **fail-closed against an explicit manifest**, tool args are schema-validated (no casing bypass), and the model-visible transcript is separated from the hidden audit trace. Per your recommended order, items 1–3 are done; **wiring/calibrating the ATO-P13 judge (and implementing P05–P12) is what remains before anything can be promoted to certified gold.**
