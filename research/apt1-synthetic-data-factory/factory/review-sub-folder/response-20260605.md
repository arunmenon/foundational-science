# Response to review-20260605-092711-IST

All findings actioned. Verified: `python3 run.py` → 4/4; `python3 -m pytest -q` → 3 passed.

| # | Finding | Resolution | Status |
|---|---------|-----------|--------|
| 1 (HIGH) | ATO-P03 bypassable by skipping the discovery lookup | `attacker_controlled_channels` now **materialized from scenario ground truth at env start** (`env.py __init__`), fail-closed and independent of agent discovery. Added regression `ato-breach-skip-lookup` → FAILS at step 1. | ✅ fixed |
| 2 (HIGH) | Field drift: checker reads `channel`, canon is `channel_target` | `p03` reads `channel_target` (legacy `channel` fallback); env/goldens emit `channel_target`; **missing channel fails closed**. Added regression `ato-breach-no-channel` → FAILS. Canonical `channel_target="SMS"` breach → FAILS at step 4. | ✅ fixed |
| 3 (MED) | `CaseState` simplified vs contract §2 | Added `ts` to evidence + actions_taken; renamed `params`→`args` in actions_taken; enriched `attacker_controlled_channels` to `{channel, changed_at_hours}`; **docstring relabeled "REDUCED in-memory model"** with a normalization-boundary note. Full 1:1 alignment deferred to pack.yaml/normalization layer (the option the review allowed). | ✅ addressed |
| 4 (LOW) | Hard-coded golden denominator | `run.py` materializes `cases` and prints `len(cases) - failures` / `len(cases)`. | ✅ fixed |
| 5 (TEST GAP) | Missing robustness fixtures | Added `test_p03_cannot_be_bypassed` covering skip-lookup, canonical-`channel_target`, and no-channel fail-closed; all assert FAIL-on-ATO-P03 with `task_success` still True. | ✅ fixed |
| note | `python` not aliased | README now uses `python3` with a note. | ✅ fixed |

P09-robustness question (review F5 bullet 4): in `ato-breach-skip-lookup` the sim-swap evidence is absent (no lookup), so P09's trigger is not met and it passes vacuously — acceptable here because **ATO-P03 (hard) already fails the trajectory**. Whether "skipped required discovery" should itself be a soft policy is left as a future `ATO-Pxx` (discovery-before-step-up) per your recommendation, not relied on for P03.
