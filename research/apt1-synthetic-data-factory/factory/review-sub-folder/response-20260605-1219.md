# Response to review-20260605-121859-IST

All findings actioned. Verified: `python3 -m pytest -q` → **9 passed**; `python3 run.py` → golden **4/4**.

| # | Finding | Resolution | Status |
|---|---------|-----------|--------|
| 1 (HIGH) | `finalize_disposition` not terminal; parallel calls manufacture a PASS | (a) `env.apply` now raises `DispositionClosed` for any action after disposition; (b) generator **stops immediately** on finalize and **rejects** a turn where finalize is not the sole tool call (`terminal_reason=protocol_error:finalize_not_sole_call`); (c) `parallel_tool_calls=False` requested; (d) new hard **lifecycle checker `ATO-LIFE-01`** (disposition must be the single, last event). Tests: `test_finalize_must_be_sole_call`, `test_no_action_after_disposition_raises`. | ✅ fixed |
| 2 (HIGH) | Customer turns missing from persisted trajectory | `message` events carry `role`; generator logs the **opener and every customer reply** as `role="customer"`. Test: `test_customer_turns_are_logged`. | ✅ fixed |
| 3 (HIGH) | Eval gate can retain policy-breaching text as "gold" | Grader adds `fully_evaluated` + `certified_gold` (PASS **and** no skipped policy). `generate.py` writes **certified gold vs uncertified candidates** separately; with ATO-P13 skipped, **nothing is certified gold (fail-closed)**. README relabeled. Test: `test_compliant_passes_but_not_gold_until_judge`. | ✅ fixed |
| 4 (MED) | `passes/k` mislabeled as pass^k | `compute_metrics` reports **pass@1** (mean) and **pass^k** (all-k) distinctly; README/CLI corrected. Test: `test_metrics_distinguish_pass_at_1_and_pass_pow_k`. | ✅ fixed |
| 5 (MED) | Malformed output aborts the batch | Tool **name + required-arg validation** before env; bad JSON / unknown tool / missing args → `terminal_reason=protocol_error:*` (run rejected, not raised); `generate.py` has a **per-run try/except** + atomic writes; client guards missing `choices`. Test: `test_unknown_tool_rejects_run_without_raising`. | ✅ fixed |
| 6 (LOW) | Missing provenance | Each record stores run_id, scenario, contract/pack/target versions, agent+customer model & temps, max_steps, terminal_reason, grade, skipped policies, certified flag. Hidden scenario ground truth is **not** embedded in the transcript. | ✅ fixed |

Notes:
- Kept Chat Completions for cross-provider endpoint compatibility (your point re: Responses API noted); tool args are validated by application code as the docs advise.
- Consequence of the fail-closed gate: until the **ATO-P13 LLM judge** is wired (via the same OpenAI client + judge-calibration.md), live generation will produce *candidates*, not certified gold. That is the correct, honest state.
